# CODEMAP · 项目代码结构（不含 dev/ 开发台）【项目级别】填

> **干嘛**：项目真实代码在哪、各文件夹装什么,给 agent 导航。**`dev/` 是开发台(过程/计划),不在此图**——本图只画「项目本身」(根目录下的真实代码)。
> **怎么更新**：代码结构变了(加/删/重命名模块)就同步本图;**只记结构与职责、不记实现细节**(实现看代码 + `tasks/done/`)。
> **不画什么**：`dev/`(开发 OS,含 `autonomy/` 运行契约——那是无人值守的**过程契约**,不是项目代码,别画进本图)。全 OS 文件导航见 `dev/HARNESS_INDEX.md`。

<!-- 格式·防跑偏 | 结构型(随代码结构慢变):固定两块——① 仓库根表(路径 | 装什么)；② 各代码主目录分层表(模块/文件夹 | 一句话职责)。
只记**结构 + 一句话职责**,不记实现细节(实现看代码 + tasks/done/)；`dev/` 永不画进本图。占位一律 <...> + 旁注怎么填,绝不烤进具体项目域内容。 -->

## 仓库根
| 路径 | 装什么 |
|---|---|
| `<代码主目录>` | <项目代码主体> |
| `dev/` | 开发 OS（**本图不画**） |
| `<docs/ · config/ · data/ · deploy/ ...>` | <文档 / 配置 / 数据 / 部署 / 脚本...> |

## <代码主目录>（按本项目实际分层填）
| 模块 / 文件夹 | 职责（一句话） |
|---|---|
| `<module>/` | <装什么> |

> 只画**结构 + 一句话职责**;实现细节不进本图,权威以代码为准。

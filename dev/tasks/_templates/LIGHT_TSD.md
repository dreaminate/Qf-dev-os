<!-- 格式·防跑偏 | 结构型(固定节序) | 【开发os级别·模板】勿改本文件。
     用法:低风险变更(GATES.md §1 阶梯 R0/R1,如展示型 UI / 静态配置 / 不碰正式事实)的轻档设计note。
     ⛔ 自降级闸:本档须**自证有资格当轻档**——§0 全-no 才合法;任一非 no → 不许用本档,升 TSD.md。
     造件:复制到对应目录,删本注释头再填。开头声明 provenance:
       「来自仓库模板 dev/tasks/_templates/LIGHT_TSD.md」/「基于仓库模板扩展示例」/「非仓库规定模板,仅为说明」。 -->

# <LIGHT_TSD-NNNN> · <标题>

> provenance:<来自仓库模板 dev/tasks/_templates/LIGHT_TSD.md>

## Header（wiring）
| 字段 | 值 |
|---|---|
| risk_level | `<R0|R1>` （仅这两级可用本档） |
| task_id | `<TASK-NNNN>` |
| test_evidence_refs | `<TEST-NNNN, …>` |

## 0. 低风险证明（全 NO 才可用本档 · 任一非 NO → 升 TSD.md）
> 怎么填:每行按本项目风险面定义判(定义见 RULES.project)。只要有一行不是 no,本档作废,改用 TSD.md。
| 风险面 | 命中? |
|---|---|
| 造正式事实 / 权威记录 | `<no>` |
| 有状态机 / 合法迁移约束 | `<no>` |
| 碰权限 / 鉴权 / 可见性 | `<no>` |
| 改对外 API 契约 | `<no>` |
| 需数据迁移 / 改持久结构 | `<no>` |
| 碰钱 / 信用 / 不可逆证据账 | `<no>` |
| 碰 Agent / 自动决策主体 | `<no>` |
| `<项目特定高风险面占位>` | `<no>` |

## 1. 改什么（一句话）
<做什么 + 不做什么>

## 2. 接线点
<file:符号;扩展不替换>

## 3. 轻测试
<最小验证 + 一个对抗探针(种已知坏);落 TEST-NNNN>

## 4. 升级触发清单（命中任一 → 立刻升 TSD.md，重做设计）
- 实现中发现要碰上方 §0 任一风险面
- scope 蔓延到正式事实 / 权限 / 钱信用 / 状态机
- GATES.md §2 升级地板被触发(碰 X 即 ≥R3)
- 不确定 → 取高(GATES §2),即升 TSD

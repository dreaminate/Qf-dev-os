<!-- 格式·防跑偏 | 结构型(固定节脊) | 【开发os级别·模板】勿改本文件。
     用法:高风险变更(GATES.md §1 风险阶梯 R3+,或下方 tripwire 任一 yes)的**模块设计/安全契约**。
     造件:复制到对应目录,删本注释头再填。开头声明 provenance:
       「来自仓库模板 dev/tasks/_templates/TSD.md」/「基于仓库模板扩展示例」/「非仓库规定模板,仅为说明」。
     节脊不可乱序/漏必填;域不变量编号(INV-1..n)是契约,后续测试/PR 按号反引。 -->

# <TSD-NNNN> · <模块/能力标题>

> provenance:<来自仓库模板 dev/tasks/_templates/TSD.md>

## Header（wiring · 反引上下游 id）
| 字段 | 值 |
|---|---|
| risk_level | `<R3|R4|R5>` （GATES.md §1 定） |
| task_id | `<TASK-NNNN>` |
| review_receipt_ref | `<REVIEW-NNNN —— 人审回执;agent 不可自批>` |
| test_evidence_refs | `<TEST-NNNN, …>` |
| adr_refs | `<ADR-NNNN —— 若本设计落定某架构取舍>` |
| 关联代码/数据/接口 | `<file:符号 / 表 / API 路由 占位>` |

## tripwire（任一 yes → 自动升级,不许停在低档）
> 怎么填:每项按本项目「正式事实/Agent/权限/钱信用证据」的定义判(定义见 RULES.project)。任一 yes:① risk_level 至少 R3 ② 本 TSD 必产 ③ required-tests 必含对应防护测试。
- [ ] 是否**造正式事实**(产生系统据以行动的权威记录)?  `<yes/no>`
- [ ] 是否**碰 Agent / 自动决策主体**?  `<yes/no>`
- [ ] 是否**碰权限 / 鉴权 / 可见性**?  `<yes/no>`
- [ ] 是否**碰钱 / 信用 / 不可逆证据账**?  `<yes/no>`  ← 命中即触发 RULES §双轴的「运行时不可逆门」(永不降级,人审双签)

## 1. 背景 / 动机
<解决什么真实问题;链到 finding / 需求 / gap>

## 2. 域不变量（编号 · 契约）
> 必须**永真**的命题;每条给一个 INV-id,测试与 PR 按号反引。
- **INV-1**:`<一句话不变量,如「X 的写入只追加不覆盖」>`
- **INV-2**:`<…>`

## 3. 状态机
<状态枚举 + 合法迁移 + 非法迁移要被拒;含初态/终态。无状态则写「无状态机,N/A」>

## 4. 权限 / 可见性
<谁能做什么;前端状态不得当权限信源(权限在服务端判)。占位:本项目角色/作用域>

## 5. API 契约
<入参/出参/错误码;幂等键;契约变更须同 commit 同步文档(GATES §4 硬同步)>

## 6. 数据设计
<新增/改动的数据结构;迁移影响;**正式事实账户只追加**(若 tripwire 命中)>

## 7. 滥用 / 攻击面防护
<谁会怎么滥用;对抗输入;不可信输入默认不升格为正式事实(RULES §单一源)>

## 8. 并发 / 幂等
<并发写竞争;重试/重放;幂等保证点>

## 9. 测试计划（对接 required-tests）
<针对 INV-* 与状态机/权限的对抗探针 + 变异要杀的点;每项落 TEST-NNNN 证据>

## 10. migration / 补偿
<上线迁移步骤;失败回滚 / 补偿事件;不可逆动作的人审门(若命中钱信用 tripwire)>

## 11. 风险权衡
<选这条路放弃了什么;残余风险(诚实限界 vs 诚实残余,见 RULES §诚实纪律)>

## 12. ADR 引用
<本 TSD 落定的架构决策 → ADR-NNNN;无则「无重大架构取舍」>

## 13. 变更记录（append-only）
- `<日期>` · `<改了什么 + 谁确认>`

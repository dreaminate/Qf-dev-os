# RULES.project · 本项目铁律【项目级别】

> OS 通用铁律见 `RULES.md`(勿动)。这里只写**本项目**特有的红线。
> CLAUDE.md 会把新 agent 指到这份。

<!-- 格式·防跑偏 | 结构型(【项目级别】填):固定五块——
项目红线 / 风险阶梯触发条件(接 GATES) / 正式事实枚举 / 致命错误即停工(不可逆) / 冻结区·性能·数据标准。
照 <...> 占位填本项目特有红线;OS 通用铁律不写这里(在 RULES.md)。
怎么更新：① 开发者明确给出/指明本项目红线时,加进来;② agent 遇模糊处 → 先问开发者 → 确认是项目规则后才加(agent 不自己定项目红线)。 -->

## 项目红线
- <冻结文件 / 不可改的页面或契约，如 `<前端某页/某契约>` 冻结,只可排版/显示/加字段>
- <资产/范围硬约束，如 某操作永不在生产执行 / 禁用某依赖>
- <安全不变量(开发不得削弱,由产品代码运行时强制)，如 敏感凭证不进 LLM / 关键护栏 / 权限边界>
- <单一源锚点，如 身份源在 `<某模块>`,不另造>

## 风险阶梯·每级触发条件（接 GATES.md 风险阶梯 R0–R5）
> **怎么填**:`GATES.md` §1 给的是**通用阶梯 + 必备工件**(OS 级、跨项目);**触发条件是本项目的**——把「碰哪些本项目的面 → 落到哪级」填这里,GATES §1/§2 据此读级。**碰任一敏感面即升级,不看 diff 大小;不确定取高。**
| 级 | 本项目触发条件(碰这些面 → 落本级) |
|---|---|
| R0 纯文档/注释 | <如 改文档/注释/非逻辑配置> |
| R1 低风险逻辑 | <如 局部 UI / 非契约工具函数> |
| R2 一般功能 | <如 新增非敏感接口 / 一般数据读写> |
| R3 契约/状态机 | <如 碰 <对外契约 / 核心状态机 / 权限边界>> |
| R4 敏感数据/迁移 | <如 <数据迁移 / 敏感数据 / 鉴权>> |
| R5 不可逆门 | <如 碰 <真钱 / 真信用 / 不可逆动作 / 生产 flag / 真争议裁决 / 真 KMS>——见下方致命错误枚举> |

## 正式事实枚举（formal facts —— 不可信输入不得自动升格为此）
> **怎么填**:列出本项目**算「正式事实」的具体事件/状态**(正式事实=可证/可测/可逆-或可补偿/可审,见 RULES.md §单一源 + §不可信输入)。外部/客户端/对话输入未经服务端校验**不得**成为下列任一。
- <正式事实 1，如 <某确认 / 某选定 / 某版本 / 某协议生效 / 某提交 / 某验收>>
- <正式事实 2，…>
- **升格门**:上述事实只由**服务端 trusted 信源**置真;受门禁动作(尤其不可逆门)只取 trusted 信源,不取客户端自带信源。

## 致命错误即停工（本项目定义，对应 RULES.md §5 + §双轴·运行时不可逆门）
> **怎么填**:列本项目「出现/被削弱即**立即停工、报告、不擅自继续**」的具体错误。这批 = §双轴门禁的**运行时不可逆门**项目落地,**永不降级、永远人审(双签),AI 不得自动执行**。
- <数据泄露 / 敏感凭证外泄>
- <不可逆操作绕过护栏，如 <真实资金动作 / 不可逆状态跃迁 / 生产发布 flag> 被自动执行>
- <安全不变量被削弱，如 防重放/权限边界/双签门 被关或绕>

## 冻结区 / 性能 / 数据标准（可选）
- **冻结区 glob**(改前必走协商,不擅动):`<如 frontend/src/legacy/** · contracts/**>`——怎么填:写本项目「碰前必问」的路径 glob,`validate_project.py` 的 FREEZE_GLOBS 据此提示。
- <性能:某查询 < Xs / 关键路径 SLO>
- <数据:每表 ≥N data tests + 版本 + checksum / 可复现 ±1e-6>
